---
import BaseLayout from "@layout/BaseLayout.astro";
import type { WithContext, Thing } from "schema-dts";
import { withBase } from "@utils/paths";

const service = {
  title: "Pilot to Production Delivery",
  subtitle:
    "Take a validated GenAI use case through solution design, integration, launch, and the first months of operations.",
  problem:
    "Teams can build a demo but struggle with integration, reliability, security reviews, and stakeholder alignment.",
  whatWeDo: [
    "Refine the problem statement, value hypothesis, and launch metrics",
    "Design the end-to-end solution and architecture, including retrieval, evaluation, and guardrails",
    "Implement, integrate, and harden the application with monitoring and safety controls",
    "Support launch, incident drills, and the first phase of lifecycle management",
  ],
  deliverables: [
    "Detailed solution design, architecture documents, and integration specs",
    "Working GenAI application deployed in a production environment",
    "Monitoring, evaluation, and safety controls with dashboards and alerts",
    "Runbooks, playbooks, and handover sessions for internal teams",
  ],
  timeline: "Typical engagement: 8-12 weeks, depending on complexity",
};

const cta = {
  text: "Plan a delivery sprint",
  link: "/contact",
};

const seo = {
  title: "Pilot to Production Delivery | Accelyze",
  description:
    "Accelyze engineers and program leads turn GenAI pilots into production deployments with architecture, integration, and runbooks.",
};

const schema: WithContext<Thing> = {
  "@context": "https://schema.org",
  "@type": "WebPage",
  inLanguage: "en-US",
  "@id": Astro.url.href,
  url: Astro.url.href,
  name: service.title,
  description: seo.description,
  isPartOf: {
    "@type": "WebSite",
    url: `${Astro.site}`,
    name: "Accelyze",
    description:
      "Accelyze helps enterprises design, build, and operate GenAI systems that make it into production.",
  },
};
---

<BaseLayout seo={seo} schema={schema}>
  <section class="mx-auto max-w-[85rem] px-4 pb-12 pt-48 sm:px-6 lg:px-8">
    <div class="max-w-4xl">
      <p class="text-sm font-semibold uppercase tracking-[0.3em] text-indigo-600 dark:text-indigo-400">
        Services
      </p>
      <h1 class="mt-4 text-balance text-4xl text-slate-800 sm:text-5xl md:text-6xl lg:text-7xl dark:text-white">
        {service.title}
      </h1>
      <p class="mt-6 text-lg text-slate-600 dark:text-slate-300">
        {service.subtitle}
      </p>
    </div>
  </section>

  <section class="mx-auto max-w-[85rem] px-4 pb-16 sm:px-6 lg:px-8">
    <div class="rounded-3xl border border-slate-200 bg-white/90 p-8 shadow-lg dark:border-slate-800 dark:bg-slate-900/80">
      <h2 class="text-xl font-semibold text-slate-900 dark:text-white">The challenge</h2>
      <p class="mt-4 text-base text-slate-600 dark:text-slate-300">
        {service.problem}
      </p>
    </div>

    <div class="mt-8 rounded-3xl border border-slate-200 bg-white/90 p-8 shadow-lg dark:border-slate-800 dark:bg-slate-900/80">
      <h2 class="text-xl font-semibold text-slate-900 dark:text-white">What we do</h2>
      <ul class="mt-4 list-disc space-y-2 pl-5 text-base text-slate-600 dark:text-slate-300 marker:text-indigo-500">
        {service.whatWeDo.map((item) => (
          <li>{item}</li>
        ))}
      </ul>
    </div>

    <div class="mt-8 rounded-3xl border border-slate-200 bg-white/90 p-8 shadow-lg dark:border-slate-800 dark:bg-slate-900/80">
      <h2 class="text-xl font-semibold text-slate-900 dark:text-white">Deliverables & outcomes</h2>
      <ul class="mt-4 list-disc space-y-2 pl-5 text-base text-slate-600 dark:text-slate-300 marker:text-indigo-500">
        {service.deliverables.map((item) => (
          <li>{item}</li>
        ))}
      </ul>
      <p class="mt-6 text-sm font-semibold uppercase tracking-wide text-indigo-600 dark:text-indigo-400">
        {service.timeline}
      </p>
    </div>
  </section>

  <section class="mx-auto max-w-4xl px-4 pb-24 text-center sm:px-6 lg:px-8">
    <div class="rounded-3xl border border-slate-200 bg-white/90 p-10 shadow-2xl shadow-indigo-200/40 dark:border-slate-800 dark:bg-slate-900/80">
      <h2 class="text-3xl font-semibold text-slate-900 dark:text-white">
        Ready to move past the demo?
      </h2>
      <p class="mt-4 text-base text-slate-600 dark:text-slate-300">
        We’ll map the remaining work, own delivery, and transfer everything to your teams with runbooks and metrics.
      </p>
      <a
        href={withBase(cta.link)}
        class="mt-8 inline-flex items-center justify-center rounded-xl bg-indigo-600 px-6 py-3 text-sm font-semibold text-white shadow-lg shadow-indigo-600/30 transition hover:bg-indigo-500 dark:bg-indigo-500 dark:hover:bg-indigo-400"
      >
        {cta.text}
      </a>
    </div>
  </section>
</BaseLayout>
