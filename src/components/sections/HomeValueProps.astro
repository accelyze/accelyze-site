---
import { withBase } from "@utils/paths";

const pillars = [
  {
    title: "Production grade from day one",
    description:
      "We design with prod in mind: observability, cost controls, fallback paths, human in the loop, and security from the first sprint. No handoff from a prototype team that never thought about uptime.",
  },
  {
    title: "Full stack AI delivery",
    description:
      "Our team combines data science, ML engineering, and MLOps. We handle model selection, prompt and retrieval design, pipelines, infrastructure as code, and integration with your existing systems.",
  },
  {
    title: "Business value that holds up in the boardroom",
    description:
      "We align on a concrete outcome at the start—handling time, conversion, fewer manual touches—and implement measurement so the value can be defended.",
  },
];
---

<section class="bg-off-white py-16 dark:bg-slate-950 sm:py-24">
  <div class="mx-auto max-w-[85rem] px-4 sm:px-6 lg:px-8">
    <div class="mx-auto max-w-4xl text-center">
      <p class="text-sm font-semibold uppercase tracking-[0.3em] text-indigo-600 dark:text-indigo-300">
        Value Proposition
      </p>
      <h2 class="mt-4 text-3xl font-semibold text-slate-900 sm:text-4xl dark:text-white">
        Why enterprises call Accelyze
      </h2>
      <p class="mt-4 text-lg text-slate-600 dark:text-slate-300">
        Most organizations have already experimented with AI. Slide decks and proofs of concept are not the bottleneck. The real gap is taking AI to production safely, reliably, and on time. This is the space where Accelyze operates.
      </p>
    </div>

    <div class="mt-12 grid gap-6 md:grid-cols-2 lg:grid-cols-3">
      {pillars.map((pillar, index) => (
        <div class="flex flex-col rounded-2xl border border-slate-200 bg-white/80 p-6 shadow-sm dark:border-slate-800 dark:bg-slate-900/70">
          <span class="text-sm font-semibold uppercase tracking-wide text-slate-400">
            Pillar {index + 1}
          </span>
          <h3 class="mt-3 text-xl font-semibold text-slate-900 dark:text-white">
            {pillar.title}
          </h3>
          <p class="mt-3 text-base text-slate-600 dark:text-slate-300">
            {pillar.description}
          </p>
        </div>
      ))}
    </div>

    <div class="mt-10 text-center">
      <a
        href={withBase("/services")}
        class="inline-flex items-center text-base font-semibold text-indigo-700 transition hover:text-indigo-500 dark:text-indigo-300"
      >
        Learn more about our services
        <svg
          class="ms-2 size-4"
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
        >
          <path d="M5 12h14" />
          <path d="m12 5 7 7-7 7" />
        </svg>
      </a>
    </div>
  </div>
</section>
