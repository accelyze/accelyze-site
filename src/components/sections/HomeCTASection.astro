---
import Button from "@ui/Button.astro";
import { withBase } from "@utils/paths";
---

<section class="bg-linear-to-r/oklch from-indigo-900 via-indigo-700 to-indigo-500 py-16 text-white sm:py-24">
  <div class="mx-auto max-w-[85rem] px-4 sm:px-6 lg:px-8">
    <div class="rounded-3xl border border-white/20 bg-white/5 p-10 text-center shadow-2xl backdrop-blur">
      <p class="text-sm font-semibold uppercase tracking-[0.3em] text-amber-200/80">
        Let’s talk
      </p>
      <h2 class="mt-4 text-balance text-3xl font-semibold sm:text-4xl">
        Ready for AI that ships
      </h2>
      <p class="mt-4 text-lg text-white/80">
        If you are tired of slides and pilots and want a production launch plan, we should talk. A short discovery call is usually enough to tell whether Accelyze is a fit and what a first project could look like.
      </p>

      <div class="mt-8 flex flex-col items-center gap-4 sm:flex-row sm:justify-center">
        <Button href={withBase("/contact")} className="text-base">
          Schedule a discovery call
        </Button>
        <a
          href={withBase("/case-studies")}
          class="inline-flex items-center text-base font-semibold text-white transition hover:text-amber-200"
        >
          See case studies
          <svg
            class="ms-2 size-4"
            xmlns="http://www.w3.org/2000/svg"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
          >
            <path d="M5 12h14" />
            <path d="m12 5 7 7-7 7" />
          </svg>
        </a>
      </div>
    </div>
  </div>
</section>
